//! ForgeDB integration: models, catalog fetching with cascade fallback,
//! and hardware auto-detection via fingerprints.

#![allow(dead_code)]

pub mod catalog;
pub mod fingerprint;
pub mod models;
