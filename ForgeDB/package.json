{
  "name": "forgedb",
  "version": "2.0.0",
  "private": true,
  "description": "ForgeDB - Hardware Database for multi-forge",
  "scripts": {
    "build": "node scripts/build-catalog.js",
    "validate": "node scripts/validate.js",
    "validate:device": "node scripts/validate.js --type device",
    "validate:image": "node scripts/validate.js --type image"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0"
  }
}
